<section>
  <div class="w-full relative flex justify-center">
    <div class="absolute inset-0 w-full md:w-1/2 h-full bg-no-repeat bg-cover bg-center" style="background-image: url('{{ entry.projectFeaturedImage.one().url }}')">

    </div>
    <div class="container h-6/10-screen flex justify-center z-10">
      <div class="w-0 md:w-1/2"></div>
      <div class="w-full md:w-1/2 h-full px-10 flex flex-col justify-center items-center md:items-start text-center md:text-left text-white md:text-black">
        <h2 class="text-6xl leading-tight">{{ entry.title }}</h2>
        <p class="font-black mt-5">{{ entry.projectDate|date('d /M /Y', timezone="Australia/Melbourne")}}</p>
        <p class="mt-10">{{ entry.projectDescription }}</p>
      </div>
    </div>
  </div>
</section>
